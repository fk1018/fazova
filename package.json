{
  "name": "fazova",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^15.0.2",
    "typescript": "^4.2.4"
  },
  "devDependencies": {
    "concurrently": "^6.1.0",
    "nodemon": "^2.0.7",
    "rimraf": "^3.0.2"
  },
  "scripts": {
    "build":"rimraf dist && tsc",
    "dev": "concurrently \"tsc --watch\" \"nodemon -q dist/index.js\""
  }
}
